# git_test

# "Hello Odin!"

A simple repository created to practice and test the basic Git workflow.

## Purpose

This repository is used to practice:

- Creating a repository
- Adding files
- Staging changes with `git add`
- Creating commits with `git commit`
- Pushing changes with `git push`
- Modifying and tracking files
- Renaming files
- Updating commit history

## Learning

I am learning web development and Git using [The Odin Project](https://www.theodinproject.com/).

## Git Commands Practiced

```bash
git add .
git commit -m "Commit message"
git status
git log
git push
git pull
